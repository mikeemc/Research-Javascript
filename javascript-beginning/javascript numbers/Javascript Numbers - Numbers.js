// Task One

const secsPerMin = 60;
const minsPerHour = 60;
const hoursPerDay = 24;
const daysPerWeek = 7;
const weeksPerYears = 52;

const secondsPerDay = secsPerMin * minsPerHour * hoursPerDay;
console.log(`There are ${secondsPerDay} seconds in a day`);

const age = 32;

const yearsAlive = secondsPerDay = secsPerMin * minsPerHour * hoursPerDay * daysPerWeek * weeksPerYears * age;
console.log(`I've been alive for more than ${yearsAlive} seconds!`);

// Task Two

const HTMLBadges = prompt('How many HTML badges do you have?');
const CSSBadges = prompt('How many CSS badges do you have?');

const totalBadges = parseInt(HTMLBadges) + parseInt(CSSBadges)'
console.log(totalBadges);

// Task Three
